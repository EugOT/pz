#!/usr/bin/env bun
// Direct provider API calls are disabled by policy in this fork. Use an
// approved Claude CLI/agent adapter through PZ_PROVIDER_CMD instead.

await Bun.stdin.text();

process.stdout.write(
  "err:direct Anthropic API bridge disabled by policy; configure PZ_PROVIDER_CMD with an approved Claude CLI adapter\n",
);
process.stdout.write("stop:err\n");
